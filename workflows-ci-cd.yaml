name: CI/CD Loja Veloz

on:
  push:
    branches: [ "main" ]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3

      - name: Build Docker
        run: |
          docker build -t pedidos ./services/pedidos
